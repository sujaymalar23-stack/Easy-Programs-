#include <stdio.h>

int main() {
    int N, i;

    // Input
    printf("Enter the value of N: ");
    scanf("%d", &N);

    // Printing first N natural numbers
    printf("First %d natural numbers:\n", N);
    for (i = 1; i <= N; i++) {
        printf("%d ", i);
    }

    return 0;
}
